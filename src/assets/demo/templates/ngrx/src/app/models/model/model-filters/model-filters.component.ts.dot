import {Component, Input, OnInit} from '@angular/core';
import {UserSearchParams} from '../{{=it.m.names.hyphen}}-search-params';

@Component({
  selector: 'anms-{{=it.m.names.hyphen}}-filters',
  templateUrl: './{{=it.m.names.hyphen}}-filters.component.html',
  styleUrls: ['./{{=it.m.names.hyphen}}-filters.component.scss']
})
export class UserFiltersComponent implements OnInit {

  /**
   * The filters
   *
   * @type {UserSearchParams}
   */
  @Input() {{=it.m.names.lowerCamel}}SearchParams: {{=it.m.names.upperCamel}}SearchParams = new UserSearchParams();

  /**
   * Constrcutor
   */
  constructor() {
  }

  /**
   * Init
   */
  ngOnInit() {
  }

  /**
   * Called when an input is changed
   */
  onChange() {
    this.{{=it.m.names.lowerCamel}}SearchParams.next();
  }

}
