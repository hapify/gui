import {Component, Input, OnInit} from '@angular/core';
import {UserSearchParams} from '../user-search-params';

@Component({
  selector: 'anms-user-filters',
  templateUrl: './user-filters.component.html',
  styleUrls: ['./user-filters.component.scss']
})
export class UserFiltersComponent implements OnInit {

  /**
   * The filters
   *
   * @type {UserSearchParams}
   */
  @Input() userSearchParams: UserSearchParams = new UserSearchParams();

  /**
   * Constrcutor
   */
  constructor() {
  }

  /**
   * Init
   */
  ngOnInit() {
  }

  /**
   * Called when an input is changed
   */
  onChange() {
    this.userSearchParams.next();
  }

}
