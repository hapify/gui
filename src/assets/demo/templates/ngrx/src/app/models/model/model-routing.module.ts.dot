import {NgModule} from '@angular/core';
import {Routes, RouterModule} from '@angular/router';

import {{{=it.m.names.upperCamel}}Component} from './{{=it.m.names.hyphen}}/{{=it.m.names.hyphen}}.component';
import {{{=it.m.names.upperCamel}}CreateComponent} from './views/{{=it.m.names.hyphen}}-create/{{=it.m.names.hyphen}}-create.component';
import {{{=it.m.names.upperCamel}}ReadComponent} from './views/{{=it.m.names.hyphen}}-read/{{=it.m.names.hyphen}}-read.component';
import {{{=it.m.names.upperCamel}}EditComponent} from './views/{{=it.m.names.hyphen}}-edit/{{=it.m.names.hyphen}}-edit.component';
import {{{=it.m.names.upperCamel}}ListComponent} from './views/{{=it.m.names.hyphen}}-list/{{=it.m.names.hyphen}}-list.component';

const routes: Routes = [
  {
    path: '{{=it.m.names.hyphen}}',
    component: {{=it.m.names.upperCamel}}Component,
    children: [
      {
        path: '',
        component: {{=it.m.names.upperCamel}}ListComponent,
        data: {
          title: 'List {{=it.m.names.wordsLower}}'
        }
      },
      {
        path: 'create',
        component: {{=it.m.names.upperCamel}}CreateComponent,
        data: {
          title: 'Create {{=it.m.names.wordsLower}}'
        }
      },
      {
        path: 'edit/:id',
        component: {{=it.m.names.upperCamel}}EditComponent,
        data: {
          title: 'Edit {{=it.m.names.wordsLower}}'
        }
      },
      {
        path: ':id',
        component: {{=it.m.names.upperCamel}}ReadComponent,
        data: {
          title: 'Read {{=it.m.names.wordsLower}}'
        }
      },
    ]
  }
];

@NgModule({
  imports: [RouterModule.forChild(routes)],
  exports: [RouterModule]
})
export class {{=it.m.names.upperCamel}}RoutingModule {
}
